# ux/static/contact.py

# Use ux/parent.py as parent (nested)

# contact us  view ux

# which gets, name, email, phone (optional), github username (optional), message
# and sends email of user request to info@q-e.io